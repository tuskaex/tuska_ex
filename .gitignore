# Dependencies
node_modules/
**/node_modules/

# Next.js
.next/
out/
**/.next/

# Python
venv/
.venv/
**/__pycache__/
*.py[cod]
*$py.class
*.so
.Python
**/venv/

# Env & secrets (never commit)
.env
.env.*
!.env.example
*.pem

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
.cursor/

# Logs & debug
*.log
*.txt.log
admin_logs.txt
npm-debug.log*
yarn-debug.log*

# Upload storage (use object storage in production)
/uploads/
**/uploads/

# Database backups
/backups/

# Test / coverage
.coverage
htmlcov/
.pytest_cache/
.mypy_cache/

# Build
dist/
build/
*.egg-info/

# Misc
*.local
*.tsbuildinfo

# Claude Code agent state (never commit)
.claude/

# Local working notes (extracted client doc text, scratch files)
.clientdocs/

# Binary specs / drafts / office docs — kept locally, not tracked
*.docx
*.doc
*.pdf
*.xlsx
*.xls
*.ppt
*.pptx
*.zip
*.tar
*.tar.gz
*.tgz
*.7z
*.rar
*.bak
~$*

# Client reference mockups dropped at the repo root (kept locally only)
/ChatGPT Image*.png
/ChatGPT Image*.jpg
tuskaex_web/

# Source APK drop-zone — redundant with the served copy at
# frontend/trader/public/downloads/tuskaex1.apk. Keep locally, don't track.
# (Scoped to the root folder only so the served public/ APK stays tracked.)
/apk/

# TradingView Charting Library download (licensed — do NOT commit)
charting_library-master/
backend/services/market_data/

# ---- SpeedTrade ----
# A separate product with its own private repo. It lives inside this working
# tree for convenience only; this repo is PUBLIC, so it must never be committed
# here. Its own .git is what tracks it.
/speedtrade_landing/
