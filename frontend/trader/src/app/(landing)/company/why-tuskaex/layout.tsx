import type { ReactNode } from 'react'

export const metadata = {
  title: 'Why TuskaEx — Professional Trading Platform',
  description: 'Precision-engineered trading platform with institutional-grade infrastructure.',
}

export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
