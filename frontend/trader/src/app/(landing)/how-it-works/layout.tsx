import type { ReactNode } from 'react'

export const metadata = {
  title: 'How It Works',
  description: 'From signup to your first trade in minutes. See how TuskaEx works end-to-end.',
}

export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
